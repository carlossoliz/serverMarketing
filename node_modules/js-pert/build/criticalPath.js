"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const network_1 = require("./network");
const getCriticalPath = (network, slack) => {
    let currentNode = network_1.START;
    const criticalPath = [];
    while (currentNode !== network_1.END && currentNode !== undefined) {
        const newNode = network[currentNode]
            ? network[currentNode].successors.find(node => slack[node] === 0)
            : undefined;
        if (!!newNode) {
            currentNode = newNode;
            if (currentNode !== network_1.END) {
                criticalPath.push(currentNode);
            }
        }
        else {
            throw 'No critical path found';
        }
    }
    return criticalPath;
};
exports.default = getCriticalPath;
//# sourceMappingURL=criticalPath.js.map